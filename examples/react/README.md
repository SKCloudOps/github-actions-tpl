# React Example

This directory shows how a consuming React application can reference the reusable workflows in this repository.

- Use `react-pr.yml` for pull requests.
- Use `react-main.yml` for production builds and static-site promotion.
- Keep build and deploy secrets in GitHub environments and repository secrets.

