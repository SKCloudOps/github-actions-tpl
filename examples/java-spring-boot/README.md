# Java Spring Boot Example

This directory shows how a consuming Java application can reference the reusable workflows in this repository.

- Use `java-pr.yml` for pull requests.
- Use `java-main.yml` for protected branch deployments.
- Point the build to a Spring Boot application with Maven or the Maven wrapper.

